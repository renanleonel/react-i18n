# React I18n Example

Minimal i18n setup for React using **Vite**, **TypeScript**, **react-i18next**, **shadcn/ui**, and **TailwindCSS**. Language preference is persisted in a cookie via **js-cookie**.

## Features

- 🌐 Internationalization with `react-i18next`
- ⚡️ Vite + TypeScript
- 🎨 Shadcn UI + TailwindCSS
- 🍪 Language stored in a cookie
- 🧠 Auto-detection with `i18next-browser-languagedetector`
- 📁 Supports auto-loading of `.json` translation files by namespace

## Benefits

Translation files can be scoped by feature, and any new `.json` added is automatically loaded and usable without manual import or registration. This allows easy modularization and scaling of translations across the app.

## Getting Started

```bash
pnpm install
pnpm dev
```
